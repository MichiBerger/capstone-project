import KidsForm from './KidsForm.js';

export default {
  title: 'components/KidsForm',
  component: KidsForm,
};

const Template = args => <KidsForm {...args} />;

export const Default = Template.bind({});
