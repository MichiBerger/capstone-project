import PhraseForm from './PhraseForm.js';

export default {
  title: 'components/PhraseForm',
  component: PhraseForm,
};

const Template = args => <PhraseForm {...args} />;

export const Default = Template.bind({});

